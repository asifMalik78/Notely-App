{
  "name": "notels",
  "version": "1.0.0",
  "private": true,
  "description": "Notels - A Kanban Board Application",
  "scripts": {
    "dev": "docker-compose up",
    "dev:build": "docker-compose up --build",
    "docker:down": "docker-compose down",
    "docker:clean": "docker-compose down -v --rmi local"
  }
}
